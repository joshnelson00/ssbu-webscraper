name: SSBU Webscraper

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: runwebscraper
        run: python3 scraper.py
        shell: bash

      